import type { Meta, StoryObj } from "@storybook/react";
import { {{GetFileName name}} } from "./{{GetFileName name}}";

const meta: Meta<typeof {{GetFileName name}}> = {
  title: "Components/{{GetPath name}}/{{GetFileName name}}",
  component: {{GetFileName name}},
};

export default meta;

export const Default: StoryObj<typeof {{GetFileName name}}> = {
  args: {},
};